from setuptools import find_packages, setup

setup(
    name='ctCustomRedis',
    packages=find_packages(include=['ctCustomRedis']),
    version='0.1.8',  # Increment version
    description='Common custom library for Redis',
    author='credenti',
    install_requires=[],
    )
