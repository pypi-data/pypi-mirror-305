[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "fairground"
authors = [{name = "Coefficient Systems", email = "john@coefficient.ai"}]
dynamic = ["version", "description"]
readme = "readme.md"
