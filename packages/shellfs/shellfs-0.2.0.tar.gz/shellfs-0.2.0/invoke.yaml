# =====================================================
# INVOKE CONFIGURATION:
# =====================================================
# -- ON WINDOWS:
# run:
#   echo: true
#   pty:  false
#   shell: C:\Windows\System32\cmd.exe
# =====================================================

run:
    echo: true

cleanup_all:
    extra_directories:
      - build
      - dist
      - .cache
      - .hypothesis
      - .pytest_cache
      - .ruff_cache
      - ".venv*"
      - ".tox"
