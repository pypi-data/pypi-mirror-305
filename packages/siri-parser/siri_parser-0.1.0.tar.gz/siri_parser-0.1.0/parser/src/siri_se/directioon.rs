

#[derive(Debug)]
pub struct Direction {
    pub direction_ref: Option<DirectionRef>,   // Optional reference to the direction
    pub direction_name: Option<String>,         // Name of the direction
}