[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "financial_report_cu"
version = "0.1.0"
description = "Пакет для генерации отчета о доходах и расходах"
authors = [
    { name="Your Name", email="your.email@example.com" }
]
dependencies = ["pandas"]