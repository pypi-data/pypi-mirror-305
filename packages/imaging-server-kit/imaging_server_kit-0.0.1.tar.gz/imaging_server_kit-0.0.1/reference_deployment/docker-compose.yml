version: "3"

services:
  servers_registry:
    build:
      context: ./registry
    ports:
      - "7000:8000"

  skimage:
    build:
      context: ./servers/serverkit-skimage
    depends_on:
      - servers_registry
