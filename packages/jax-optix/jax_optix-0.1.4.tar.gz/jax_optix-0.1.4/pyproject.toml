[project]
name = "jax-optix"
version = "0.1.4"
description = "Zero-overhead functional lensing for JAX PyTrees"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
authors = [
    { name = "Jonas Köhler", email = "jonas.koehler.ks@gmail.com" }
]
keywords = ["jax", "equinox", "lens", "functional", "optics"]
dependencies = [
    "equinox>=0.11.8",
    "jax>=0.4.35",
    "pytest>=8.3.3",
]

[tool.hatch.build.targets.wheel]
packages = ["src/optix"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.urls]
Homepage = "https://github.com/jonkhler/optix"
