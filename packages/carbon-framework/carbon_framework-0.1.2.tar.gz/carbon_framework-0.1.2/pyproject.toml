[tool.poetry]
name = "carbon-framework"
version = "0.1.2"
description = "Yet another python framework"
authors = ["Mahdi Zareie"]

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
