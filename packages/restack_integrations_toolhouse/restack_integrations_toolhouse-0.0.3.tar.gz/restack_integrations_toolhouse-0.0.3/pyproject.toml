[tool.poetry]
name = "restack_integrations_toolhouse"
version = "0.0.3"
description = "Toolhouse integration for Restack AI"
authors = [
    "Restack Team <service@restack.io>"
]
packages = [{ include = "restack_integrations_toolhouse", from = "src" }]

readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
pydantic = "^2.9.2"
toolhouse = "^1.3.0"
restack_ai = "^0.0.11"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
