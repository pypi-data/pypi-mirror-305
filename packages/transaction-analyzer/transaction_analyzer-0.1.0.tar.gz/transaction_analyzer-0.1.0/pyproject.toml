[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "transaction_analyzer"
version = "0.1.0"
description = "A package for analyzing financial transactions."
authors = [
    { name="Alex", email="haylarrr@yandex.ru" }
]
dependencies = ["pandas"]
[tool.setuptools]
py-modules = []