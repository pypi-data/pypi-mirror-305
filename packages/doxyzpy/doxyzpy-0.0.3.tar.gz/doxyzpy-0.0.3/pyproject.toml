[project]
name = "doxyzpy"
version = "0.0.3"
description = "doxyz组织下的py库根节点，形如doxyzpy前缀的包名烦请别冲突"
requires-python = ">=3.11"
readme = "README.md"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool]
