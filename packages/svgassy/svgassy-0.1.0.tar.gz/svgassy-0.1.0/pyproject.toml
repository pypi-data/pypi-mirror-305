[tool.poetry]
name = "svgassy"
version = "0.1.0"
description = ""
authors = ["Marek <marek@mp76.pl>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[tool.poetry.group.dev.dependencies]
pytest = "^8.3.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
