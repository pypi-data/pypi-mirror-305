# `macrenew`

The `macrenew` library is a simple library designed to change the **MAC ADDRESS** of a given network interface.

Version 1.0.0 is working with Linux Systems, but the goal is to expand this to Windows to be able to easily change the MAC ADDRESS of a Windows System.

## Usage

ToDo